name: Playwright Tests
on: [pull_request]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
      pull-requests: write
    steps:
        - uses: actions/checkout@v5
        - name: Set up Python
          uses: actions/setup-python@v6
          with:
              python-version: '3.13'
        - name: Install dependencies
          run: |
              python -m pip install --upgrade pip
              pip install -r requirements.txt
        - name: Ensure browser dependencies are installed
          run: |
              python -m playwright install --with-deps
        - name: set token for playwright test
          run:
           export GITHUB_TOKEN=${{ secrets.TOKEN }}
           export GITHUB_USER=${{ secets.USER }}
        - name: Run Playwright tests
          run: |
            python -m pytest --junitxml=test-results/playwright-results.xml
        - name: Publish Playwright test results
          uses: dorny/test-reporter@v1
          if: ${{ !cancelled() }}
          with:
            name: Playwright Tests
            path: test-results/playwright-results.xml
            reporter: java-junit